
node_C/C_cycle_test.py 에서 레시피별 시간 조정 하면서 pump_control만 해볼수 있도록 테스트 함수 별도로 하나 만들어줘봐
시


bottle_on_the_conveyor_manual_rear
bottle_on_the_conveyor_auto_rear

bottle drop_one을 위한 사전 단계에 대한 이해
bottle이 front, rear케이지에 적층 되어있는데... _rear없는 함수는 front케이지에 쌓여 있는 bottle을 한단 내려 컨베어에 올려 놓는 동작이고
bottle이 front, rear케이지에 적층 되어있는데... _rear함수는 rear케이지에 쌓여 있는 bottle을 한단 내려 컨베어에 올려 놓는 동작임
servo가 전후를 오가며 front, rear의 케이지 선택이 됨
gripper가 release되면 컨베이어에 최대 12개의 bottle이 일정간격으로 놓이게 됨, 상황에 따라 중간 중간 bottle이 빠져 있을수도 있고
이렇게 컨베이어 상에 놓인 병을 drop_one 하는것임
front를 drop_one했는데, time_out이 나면 front케이지는 bottle이 없는 상태이고
rear케이지의 bottle을 drop_one 하는데  time_out이 나면 rear 케이지에도 bottle이 없는 상태가 되어 전체 케이지에 bottle이 없는 상태가
되어 bottle none을 알림
bottle_on_the_conveyor_manual  은 front 케이지에 bottle을 채우기 위해 수동 조작하는 함수이고
bottle_on_the_conveyor_manual_rear 은 rear 케이지에 bottle을 채우기 위해 수동 조작하는 함수임



device/node_A/NODE_A_client.py에서 S3이 입력 되면
bottle_on_the_conveyor_manual_rear 실행되도록


device/NODE_test_server.py 에서 프로토콜 받아서 자동 1 cycle 수행 process를 함수 기준으로만 리스트업 해줘봐

run_make_cycle() 내부 호출 순서만 다시 펴면:

hooks["on_start"] -> KioskBridge.on_job_start()
device/NODE_test_server.py (line 550)

ensure_node_a_drop_one()
device/NODE_test_server.py (line 226)

send_and_wait("NODE_C1", "bottle.go_printer")
device/NODE_test_server.py (line 210)

hooks["on_printer_arrived"] -> KioskBridge.on_job_printer_arrived()
device/NODE_test_server.py (line 554)

copy_print_image()
device/NODE_test_server.py (line 285)

wait_printer_finish()
device/NODE_test_server.py (line 294)

wait_node_connected("NODE_C1")
device/NODE_test_server.py (line 275)

send_and_wait("NODE_C1", "bottle.pick")
device/NODE_test_server.py (line 210)

wait_node_connected("NODE_C")
device/NODE_test_server.py (line 275)

make_fill_payload(flavor)
device/NODE_test_server.py (line 323)

send_and_wait("NODE_C", "bottle.fill.{flavor}.{side}", data=...)
device/NODE_test_server.py (line 210)

send_and_wait("NODE_C1", "door.open_left") 또는 send_and_wait("NODE_C1", "door.open_right")
device/NODE_test_server.py (line 210)

wait_sensor_value("NODE_C", "S9_left_bottle", 0) 또는 wait_sensor_value("NODE_C", "S10_right_bottle", 0)
device/NODE_test_server.py (line 262)

send_and_wait("NODE_C1", "door.close_left") 또는 send_and_wait("NODE_C1", "door.close_right")
device/NODE_test_server.py (line 210)

hooks["on_done"] -> KioskBridge.on_job_done()
device/NODE_test_server.py (line 558)


/Users/admin/Desktop/Noon/device/node_C1/NODE_C1_client.py에
bottle.standby op를 추가해주고

def bottle_standby(bottle_x, mm): 함수로 처리하고
좌측으로 mm만큼 이동하는 처리해주면 되겠네...sensor처리는 없음

device/NODE_test_server.py
`cmd` : `sys.emergency_stop` 수신하면
관련 프로토콜 응답을 수행하고
NODE_A
NODE_C
NODE_C1
는 reset을 수행하도록 추가해줘... 다른 코드는 건드리지 말고 

device/NODE_test_server.py
`cmd` : `sys.self_check` 수신하면
"scope": "ALL|PRINTER|SENSOR|MOTOR|CHUTE",에 관계없이

NODE_A, NODE_C, NODE_C1 의 모든 센서값의 상태를 응답하고
프린터 상태도 회신 처리해줘

device/NODE_test_server.py
`chute.open` (door open) 수신하면
관련 처리해줘

`chute.close`도 처리해줘

`chute.open` 이후 30초후에는 `chute.close'

device/NODE_test_server.py
`cmd` : `sys.emergency_stop` 는  30초 이내에는 다시 요청들어오는거는 busy로 회신하고 무시함

device/NODE_test_server.py에서

node_C1이 처음 접속되면
bottle.pick를 한번 보내줘.

NODE_A
NODE_C
NODE_C1
의 센서 기능을 코드 상에서 파악해서 sensor.md로 저장해줘

NODE_A
NODE_C
NODE_C1
의 gpio0~15  기능을 코드 상에서 파악 해서 control.md로 저장해줘

